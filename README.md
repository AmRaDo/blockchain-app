# blockchain-app
Sample application that tracks locations using block-chain.
The location graph gets rendered on each refresh/update.

It uses truffle, web3 with MetaMask for block-chain interactions and d3 for graph rendering.
